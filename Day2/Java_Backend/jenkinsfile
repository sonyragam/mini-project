pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                #dir('/home/sony/Day2/Java_Backend') {
                    sh 'mvn clean package'
                }
            }
        }

        stage('Deploy') {
            steps {
                #dir('/home/sony/Day2/Java_Backend') {
                    sh '''
                    cd /home/sony/.jenkins/workspace/test/Day2/Java_Backend/target
                    fuser -k 9090/tcp || true
                    nohup java -jar target/*.jar > app.log 2>&1 &
                    '''
                }
            }
        }
    }
}
